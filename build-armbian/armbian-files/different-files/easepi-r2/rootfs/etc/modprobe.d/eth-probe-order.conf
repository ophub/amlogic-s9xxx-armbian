# Ensure PCI Ethernet (r8169) probes before USB RNDIS (4Gnet) and WiFi (brcmfmac)
# This keeps kernel net device registration order: eth0 → eth1 → eth2 → eth3 → wlan0 → 4Gnet
softdep rndis_host pre: r8169
softdep brcmfmac pre: r8169 brcmutil cfg80211